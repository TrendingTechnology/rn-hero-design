---
name: Button
menu: Components
---

import { Playground } from 'docz'
import { Props } from 'gatsby-theme-docz/src/components/Props';
import { Button } from 'rn-hero-design'
import { RED } from 'rn-hero-design/src/themes/hero/variables';
import { IPhoneX } from './DeviceFrame'

# Button

## Basic usage

<Playground>
  <IPhoneX screenStyle={{ padding: 16, paddingTop: 48 }}>
    <Button
      text="Filled Button"
      onPress={() => {}}
      wrapperStyle={{ marginBottom: 16 }}
    />
    <Button
      text="Loading Filled Button"
      onPress={() => {}}
      loading
      wrapperStyle={{ marginBottom: 16 }}
    />
    <Button
      text="Disabled Filled Button"
      onPress={() => {}}
      disabled
      wrapperStyle={{ marginBottom: 32 }}
    />
    <Button
      variant="outlined"
      text="Outlined Button"
      onPress={() => {}}
      wrapperStyle={{ marginBottom: 16 }}
    />
    <Button
      variant="outlined"
      text="Loading Outlined Button"
      onPress={() => {}}
      loading
      wrapperStyle={{ marginBottom: 16 }}
    />
    <Button
      variant="outlined"
      text="Disabled Outlined Button"
      onPress={() => {}}
      disabled
      wrapperStyle={{ marginBottom: 32 }}
    />
    <Button
      text="Custom Button"
      onPress={() => {}}
      wrapperStyle={{ marginBottom: 16, backgroundColor: RED, borderRadius: 8 }}
    />
  </IPhoneX>
</Playground>

## Properties

<Props props_={{
  text: { type: 'String', required: true, description: 'Display text on button' },
  onPress: { type: 'event => void', required: true },
  loading: { type: 'Boolean', required: false, defaultValue: { value: 'false' } },
  disabled: { type: 'Boolean', required: false, defaultValue: { value: 'false' } },
  variant: { type: '"filled" | "outlined"', required: false },
  wrapperStyle: { type: 'Object', required: false },
  textStyle: { type: 'Object', required: false },
  theme: { type: 'Object', required: false, description: 'The theme object contains style of all components. Can be injected by `injectTheme` HOC' },
}} />

