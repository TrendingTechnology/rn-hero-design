import { StyleSheet } from 'react-native';
import { TEXT_COLOR } from './variables';

const COLOR = TEXT_COLOR;

const styles = {
  icon: {
    color: COLOR,
  },
};

export default StyleSheet.create(styles);
