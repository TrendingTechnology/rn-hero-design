{
  "name": "react-native-hero-design",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "workspaces": {
    "packages": [
      "lib",
      "playground"
    ]
  },
  "scripts": {
    "test": "cd lib && yarn test",
    "dev": "cd playground && yarn start",
    "lib:watch": "cd lib && yarn clean && yarn watch",
    "lib:build": "cd lib && yarn build",
    "doc:dev": "cd documentation && yarn dev",
    "doc:build": "cd documentation && yarn build",
    "doc:serve": "cd documentation && yarn serve",
    "doc:deploy": "cd documentation && yarn build && yarn deploy",
    "playground:publish": "cd playground && expo publish",
    "bump": "yarn version --cwd lib"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-eslint": "^10.0.3",
    "eslint-config-prettier": "^6.5.0",
    "eslint-plugin-prettier": "^3.1.1",
    "eslint-plugin-react": "^7.16.0"
  }
}
